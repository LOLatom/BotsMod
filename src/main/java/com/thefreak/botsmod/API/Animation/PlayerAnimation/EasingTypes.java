package com.thefreak.botsmod.API.Animation.PlayerAnimation;

public enum EasingTypes {
   /* EXPONENTIAL((val, start, end) -> {
        double pct = val * val;
        return start * pct + end * (1 - pct);
    }),
    LINEAR((val, start, end) -> {
        return start * val + end * (1 - val);
    });*/
}
